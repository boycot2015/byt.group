---
import Tabs from '@/components/Movie/Tabs';
import List from '@/components/Movie/List.tsx';
import Layout from '@/layouts/Layout.astro';
import { baseApiUrl } from '@/api/index.ts';
let indexData:any = {};
// const getData = async () => {
//     let res = await fetch(baseApiUrl + '/movie');
//     let data = await res.json();
//     indexData = data.data;
// };
// await getData();
---
<Layout title='猫眼电影'>
    <Tabs client:only="react"></Tabs>
    <List client:only="react" data={indexData}></List>
</Layout>